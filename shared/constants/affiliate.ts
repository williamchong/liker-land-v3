// Books shown per voice in the public pool preview (PlusVoicesModal). The
// /plus/voices endpoint sends one extra so the client can tell more exist
// without shipping each affiliate's full whitelist to an anonymous page.
export const CIVIC_VOICES_PREVIEW_BOOK_LIMIT = 4
